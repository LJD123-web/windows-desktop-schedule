PySide6>=6.6.0

# OCR 引擎（推荐 RapidOCR，兼容性好+中文识别强）
rapidocr-onnxruntime>=1.2.0

# 备选引擎：
# pip install paddleocr paddlepaddle  (效果最好但安装体积大)
# pip install easyocr                 (兼容性好但密集表格效果一般)
# pip install pytesseract             (需额外安装 Tesseract-OCR 程序)
